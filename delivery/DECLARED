[
  {
    "scope": "author:briananderson1222 branch-prefix:agent/fable/",
    "reason": "Fieldwork terminal-state delivery arc (issues #74, #78, #79 and the v0.4.0 release bump): adopts Survey 2.4.0's queue-binding attestation, migrates the embedded workbench to the 2.3.0 DOM hooks, lands the zod 4 major, and creates this repo's Trust Verify gate and this file. These branches predate or carry the gate itself, so there is no bundle to reconcile; verification in lieu of a bundle is each PR's own CI (verify matrix on node 22/24) plus the locally-run full `npm run verify` and the 10/10 `npm run check:guards` fault-injection matrix reported on each PR. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: fieldwork terminal-state brief, item 4)",
    "declared_at": "2026-07-27T23:50:00Z"
  },
  {
    "scope": "author:dependabot[bot] branch-prefix:dependabot/",
    "reason": "Dependabot dependency-bump PR; no agent delivery involved, nothing to reconcile. Compound scope binds the platform-set actor identity to the bot's branch namespace per ADR 0022's fork-PR guidance, paired per the 2026-07-03 addendum since branch-prefix alone resolves against a pusher-controlled ref. Known and accepted cost: if the bot identity rotates this stops matching and dependency PRs break loudly, which the pinned action reports by naming the failed condition (flow-agents#1015). No release-automation entry exists because release-please does not run in this repo — fieldwork releases via a human/agent-authored version-bump PR (covered by the entry above) and a manual workflow_dispatch of publish-npm.yml at the version tag; verified against .github/workflows at adoption time.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: fieldwork terminal-state brief, item 4)",
    "declared_at": "2026-07-27T23:50:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/ts7",
    "reason": "TypeScript 7 with BOTH coupled changes tsgo demands and a bot cannot author: explicit types:[node] (tsgo no longer auto-includes @types/node — TS2591 on every node: builtin) and ambient declarations for side-effect style imports (TS2882, where tsc 5 accepted them silently). Proven pattern from the relay canary (relay#39), extended here for the browser bundle. verify:static green under the new toolchain, visual snapshots off as always on linux-parity runs. The Vite 8 half of the bot's grouped PR remains its own migration. Scope paired with author: per the ADR 0022 2026-07-03 addendum.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-28T03:30:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/tv-enforce",
    "reason": "Removes missing-bundle-policy: advisory from the Trust Verify workflow so it inherits the action default (required). Advisory was the adoption stance; with Trust Verify now a required status check in this repo's branch protection, advisory mode is a hole: a live gate probe (PR #90, deliberately carrying no bundle and no exemption) passed Trust Verify green and sat MERGEABLE. This change closes exactly that: one workflow line, no source or behaviour change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI runs the full verify matrix, and after merge the same probe PR re-runs Trust Verify and must go red — the negative proof is executed and recorded before the probe closes. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner said fine-to-arm pending working-system evidence; this is the evidence loop)",
    "declared_at": "2026-07-28T19:30:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/flow-agents-local-kit",
    "reason": "Pins @kontourai/flow-agents 5.5.0 as an exact local devDependency and adds the workflow:sidecar / workflow:validate-artifacts scripts, matching the one proven configuration (the marketing-site repo). With bundle-required now enforced portfolio-wide, this repo demanded delivery bundles while providing no declared means to author them: bundle authoring worked only on machines with a coincidental global kit install, and sandboxed delegates without network access could not produce bundles at all. Dev-tooling-only change: no source, no behaviour, no shipped-dependency change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI runs the full verify matrix, and the kit's bins resolve from the local install. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted rollout recommendation)",
    "declared_at": "2026-07-28T21:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:test/acquisition-coverage",
    "reason": "Test-only delivery for issue #89: acquireFieldwork's real crawl path had zero coverage — SSRF-pinned egress, the frontier, and snapshot acquisition never executed in any test. Adds a loopback-fixture acquisition test (real crawl, real snapshot store, run consumes the acquired snapshot), a negative test asserting forage's classified egress refusal (INVALID_PORT) with the exact production policy, and CLI coverage for the acquire verb. No production source changes. Verification in lieu of a bundle: full suite 108/108 green locally including the new files, typecheck green, and this PR's CI runs the same matrix. One disclosed gap, recorded on #89: a CLI-level success path is unreachable because the CLI wires acquireFieldwork with no egress override seam — a production-API decision deliberately left out of this test-only change. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted triage recommendations)",
    "declared_at": "2026-07-28T23:30:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:fix/surface-truncation",
    "reason": "Fieldwork half of the silent-truncation fix (#50), closing the chain after traverse 0.25.1 (honest max-chunks partial) and survey 2.4.3 (vocabulary tolerance) published: exact pin bumps to both; FieldworkRunResult gains outcome (fresh and idempotent-reopen paths); inspect surfaces outcome + warning classifications; a --max-chunks knob mirrors --max-provider-calls so the ceiling is raisable and truncation testable. Bundles one disclosed out-of-scope fix: the requestDigest validator required bare 64-hex while relay's invocationDigest emits sha256:<hex>, so every real runtime receipt failed re-read — surfaced by this change's own idempotent-reopen acceptance test and verified pre-existing by bisection against main. Agent-delivered outside builder.build in an owner-attended session, so no trust bundle exists. Verification in lieu of a bundle: full verify:static green including visual snapshots, 114/114 tests, non-JSON CLI output asserted, conformance oracles confirmed unaffected (not regenerated); independent report-only review commissioned on this diff before merge. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted triage recommendations)",
    "declared_at": "2026-07-29T00:40:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/release-0.5.0",
    "reason": "Release bump 0.4.0 to 0.5.0: version fields in package.json/package-lock.json only, cutting the release that carries #93 (real acquisition coverage) and #94 (truncation surfaced in run results/inspect/CLI plus the max-chunks knob and the requestDigest receipt fix). Minor rather than patch because #94 adds public API surface (FieldworkRunResult.outcome, --max-chunks). No source change, nothing for a bundle to reconcile; both carried changes passed independent review and full verify:static on their own PRs. Verification in lieu of a bundle: this PR's CI runs the full verify matrix at the bumped version. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted triage recommendations)",
    "declared_at": "2026-07-29T01:20:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:feat/reviewed-evidence-projection",
    "reason": "Delivers #88: fieldwork becomes the first consumer of surface 2.13's reviewed-extraction-evidence contract. reviewedExport now projects reviewed evidence per claim and evaluates the grounding policy, writing a typed receipt — allowed, refused with typed gaps, or not-evaluated for recheck rounds whose two-candidate transition shape the contract cannot express (honest disclosure, never a fabricated pass). Contract friction found during adoption is filed upstream as surface#194-197. Agent-delivered outside builder.build in an owner-attended session, so no trust bundle exists. Verification in lieu of a bundle: full verify:static green including browser and package-smoke; independent report-only review returned MERGE_OK with its MEDIUM (undocumented policy scope) and one LOW (unguarded candidate-id collision) fixed in-branch, and the remaining LOWs recorded on the PR; conformance oracle diff confirmed purely additive. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted triage recommendations)",
    "declared_at": "2026-07-29T03:20:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/release-0.6.0",
    "reason": "Release bump 0.5.0 to 0.6.0: version fields only, cutting the release that carries #97 (first-consumer adoption of surface 2.13's reviewed-extraction-evidence contract: projected evidence and a typed grounding receipt in reviewed exports). Minor because it adds public wire surface. The carried change passed independent review (MERGE_OK, findings fixed in-branch) and full verify:static on its own PR. Verification in lieu of a bundle: this PR's CI runs the full verify matrix at the bumped version. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted triage recommendations)",
    "declared_at": "2026-07-29T04:10:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/survey-bridge-adapters",
    "reason": "Closes the surface#194 loop from the consumer side: bumps the survey pin 2.4.3 to 2.5.0 and replaces the three as-unknown-as casts in reviewed-evidence.ts with survey's newly exported toSurfaceReviewedExtraction adapters, whose compile-time field-assignability guard makes cross-package shape drift fail survey's own build. Type-level change plus one exact pin bump; identical runtime behaviour (the adapters are identity functions). Verification in lieu of a bundle: typecheck green, full test suite green locally, this PR's CI runs the full verify matrix. No fieldwork release cut for this — it changes no wire output and rides the next release. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-29 session: owner said do-all on the surface/hachure recommendations)",
    "declared_at": "2026-07-29T08:20:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/pnpm-migration",
    "reason": "Wave 2 of the org-wide npm -> pnpm 11 move (canary ui#69; plan tracked from station#516). Lockfile converted with pnpm import: 285 resolved versions, all identical to package-lock.json, zero drift and zero pnpm-only additions. The optional peer @anthropic-ai/sdk IS in package-lock.json at 0.115.0, so npm installed it and autoInstallPeers stays at its default -- turning it off here would have removed a package npm had. Dependency install scripts are blocked by default with esbuild@0.28.1 the one version-pinned allowBuilds entry, added from evidence because the first strict install refused with ERR_PNPM_IGNORED_BUILDS naming it; minimumReleaseAge 1440. CI installs with pnpm install --frozen-lockfile after a SHA-pinned pnpm/action-setup in ci.yml, publish-npm.yml (three jobs) and trust-verify.yml; no workflow checks out into a subdirectory so no package_json_file is needed. Publishing stays on npm publish and npm run verify stays the verify command, so this action's contract string does not move. scripts/package-smoke.mjs invokes node_modules/.bin/tsc from the repo root, which under pnpm's isolated layout resolves only because typescript is a direct devDependency; verified present on disk and the script passes as part of verify:static via test:package, exercising the whole pack, install and bin path. No shipped source change: the packed file list is identical to the published 0.10.0 tarball (91/91) and no pnpm-* file or lockfile enters it. Agent-delivered in an owner-attended session outside builder.build, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: npm run verify green locally (typecheck, build, tests, examples, conformance, cli, api, a11y, browser, package smoke, content and decision checks, check:veritas) with npm test 216/216, and in this PR's own CI; fault injections recorded in the PR body -- a manifest/lockfile mismatch fails with ERR_PNPM_OUTDATED_LOCKFILE and removing the allowBuilds entry fails with ERR_PNPM_IGNORED_BUILDS naming esbuild@0.28.1. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-09-03 session: owner-directed pnpm migration across all repos, scoped-DECLARED-exemption route)",
    "declared_at": "2026-09-04T02:23:43Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:ci/publish-path-dry-run",
    "reason": "Adds a publish-path dry-run job to ci.yml, rolled out from kontourai/ui#70. After the org-wide npm -> pnpm 11 move, every migrated repository had proven install, build, test and pack, and none had proven publish: npm publish runs only on a tag, against a pnpm-installed tree, under a setup-node that also sets registry-url -- a combination the verify jobs never exercise, and it is that .npmrc which npm publish reads. This job mirrors that environment on every pull request and stops one step short of the registry write. It cannot publish: --dry-run performs no write, the job requests no id-token permission so provenance cannot be minted, and no registry credential is provided. Workflow-only change: no source, no dependency, no lockfile and no shipped content is touched, so nothing for a bundle to reconcile. Agent-delivered in an owner-attended session outside builder.build, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: the job is proven green on a hosted runner in ui#70, where its output rather than its exit code is the evidence -- 'npm notice total files: 289' then 'Publish path proven from a pnpm-installed tree: 289 files assembled', with the registry declining only the already-published version. Fault injections recorded there: a type error making prepack fail is reported as 'assembled no tarball'; crafted logs prove the strict branch rejects a 403 while tolerating only the named version-exists refusal, and rejects a zero-file package that exits zero. ui#70's first CI run FAILED and that is disclosed rather than hidden: GitHub runs run: blocks as bash -e, which set -uo pipefail does not clear, so npm's expected non-zero exit killed the step and every assertion below was unreachable -- the gate had no body on CI while passing locally, because it had been tested with bash rather than bash -e. The version rolled out here carries the || STATUS=$? fix and the comment explaining why. The job's action pins follow THIS repository's existing convention rather than importing ui's. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-09-04 session: owner-directed publish-path gate rollout, scoped-DECLARED-exemption route)",
    "declared_at": "2026-09-04T05:19:12Z"
  }
]
